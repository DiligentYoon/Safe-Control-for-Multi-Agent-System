name: safe_control
channels:
  - anaconda
  - defaults
  - conda-forge
dependencies:
  # --- 기본 및 데이터 사이언스 패키지 ---
  - python=3.11  
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - scipy
  - sympy
  - casadi
  
  # --- 웹 및 유틸리티 패키지 ---
  - fastapi
  - uvicorn
  - requests
  - pyyaml
  - tqdm 

  # --- pip 패키지 ---
  - pip
  - pip:
      - ffmpeg-python==0.2.0
      - future==1.0.0
      - httptools==0.6.4
      - py-spy==0.4.0
      - python-dotenv==1.1.0
      - ray==2.44.0
      - websockets==15.0.1